# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "SD3-10-layer"
BBFILE_PATTERN_SD3-10-layer := "^${LAYERDIR}/"
LAYERVERSION_SD3-10-layer = "1"
# LAYERSERIES_COMPAT_sophgo-layer = "honister kirkstone"
LAYERSERIES_COMPAT_SD3-10-layer = "langdale mickledore"
# Provide a variable that points the base of the sophgo layer.
ASPEEDSDKBASE = '${@os.path.normpath("${LAYERDIR}/")}'
